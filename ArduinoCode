#include <Servo.h>
int data = -1;

void setup(){
  Serial.begin(9200);
  pinMode(8, OUTPUT);
}

void loop(){

  if (Serial.available() > 0){
    data = Serial.read();
    if(data==1){
    	digitalWrite(8, HIGH);
    }
    if(data==0){
    	digitalWrite(8, LOW);
    }
  }

}
